/**
 * Foundathyon Accounts brand accent, in the oklch form FoundathyonProvider expects
 * (a calibrated hue/lightness, not a hex). Matches Dokgistry's orange exactly so
 * every Community product's chrome reads as the same family.
 */
export const BRAND_ACCENT = { hue: 47.6, l: 0.7049, c: 0.1867 } as const;
